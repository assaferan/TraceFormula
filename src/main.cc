#include <iostream>
#include <unordered_map>

typedef std::unordered_map<int, int> hash;

int traceFormulaGamma0(int n, int N, hash & stored_H_values)
{
  
}

int main()
{
  std::cout << "Hello world!" << std::endl;
  return 0;
}
